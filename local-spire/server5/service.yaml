apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: server5
  name: server5
spec:
  ports:
  - name: http
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app: server5
  type: ClusterIP
